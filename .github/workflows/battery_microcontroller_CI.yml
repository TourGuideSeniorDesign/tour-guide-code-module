name: Battery Microcontroller CI

on:
  workflow_dispatch:
  push:
    branches:
      - "**"
    paths:
      - "firmware/battery_microcontroller/**"
      - "firmware/shared/**"
  pull_request:
    branches:
      - "**"
    paths:
      - "firmware/battery_microcontroller/**"
      - "firmware/shared/**"

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name

    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
            firmware/battery_microcontroller/.pio/libdeps
            firmware/battery_microcontroller/.pio/build
          key: ${{ runner.os }}-pio-battery-${{ hashFiles('firmware/battery_microcontroller/platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-battery-
            ${{ runner.os }}-pio-
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install PlatformIO Core
        run: pip install platformio
      - name: Copy custom ROS interfaces
        run: |
          mkdir -p firmware/battery_microcontroller/extra_packages
          cp -r src/autogiro_interfaces firmware/battery_microcontroller/extra_packages/
      - name: Build PlatformIO Project
        run: pio run -e ROS
        working-directory: firmware/battery_microcontroller
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: battery_firmware.uf2
          path: firmware/battery_microcontroller/.pio/build/ROS/firmware.uf2
          if-no-files-found: warn
